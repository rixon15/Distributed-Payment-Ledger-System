syntax = "proto3";

package org.example.gateway.common;

option java_package = "org.example.grpc.common";
option java_multiple_files = true;

enum CurrencyType {
  CURRENCY_TYPE_UNSPECIFIED = 0;
  USD = 1;
  EUR = 2;
  RON = 3;
}

message Money {
  CurrencyType currency = 1;
  string amount = 2;
}